# Прототип боевой арены

## Границы движков

Расчёт и показ боя разделены программно:

- `battle-engine.js` — детерминированная симуляция и журнал; он не импортирует Canvas, DOM, спрайты или анимации.
- `visual-engine.js` и `sprite-library.js` — покадровый Canvas-визуал; загружается в интерфейс параллельно со скелетным ригом.
- `pixi-skeletal-engine.js` — сохранённый изолированный эксперимент PixiJS с иерархией костей; сейчас не подключён к странице.
- `app.js` — интерфейс: передаёт готовый снимок и в PixiJS-риг, и в покадровый Canvas-движок, а также отображает числовой UI.

Canvas-арена для отладки удалена. Сейчас в интерфейсе показан только
покадровый спрайтовый экран. Скелетный эксперимент PixiJS сохранён в исходниках,
но временно скрыт и не загружается. Диагностика находится в блоке
«Динамические показатели» ниже сцены: арена, seed, шаг, последнее действие,
числа бойцов, статистика и технический лог.

Для проверки исходных ассетов отдельно доступна
[`sprite-overlay.html`](sprite-overlay.html): она показывает единый лист мечника
целиком и проигрывает выбранное состояние в отдельном окне. При выборе ретиария
окно проигрывает его отдельный построчно собранный атлас с трезубцем и сетью.
Остальные классы пока используют PNG мечника.

В сцене временно ровно один подвижный компонент на бойца: `fighter`. Меч либо
трезубец с сетью уже запечены в соответствующие кадры. Это упрощение относится только к визуальному слою:
классы, предметы и их характеристики по-прежнему рассчитываются боевым движком.

Мобильный Canvas использует три самостоятельных фоновых ассета: новую стартовую
арену со зрителями, прежнюю каменную «Закрытую арену» и песчаную арену. Тип приходит из снимка боя, но сопоставление с
PNG остаётся внутри `sprite-library.js`. Поэтому боевое ядро не импортирует
фон, Canvas или правила отрисовки. Все runtime-фоны имеют размер 360×560 и
пересобираются без размытия скриптом
[`tools/build_arena_backgrounds.py`](tools/build_arena_backgrounds.py).
Поверх PNG визуальный движок рисует отдельный компактный слой пиксельного огня:
по два крупных факела на стартовой и закрытой аренах и семь небольших источников
света на песчаной. Координаты, масштаб и разные фазы хранятся в описании арены, поэтому
полноразмерный анимированный фон и его дублирование не требуются.
На новой арене поверх статичной толпы дополнительно движутся шестнадцать
полупрозрачных пиксельных зрителей: лёгкое покачивание и редкие поднятые руки
с небольшим вертикальным подпрыгиванием создают движение фона, не отвлекая от боя.

Игровой мобильный экран разделён на три независимых слоя интерфейса: верхний
DOM-HUD с названием арены, участниками, здоровьем, усталостью и травмами; Canvas 360×300 только для
фона, анимации бойцов и программной крови, масштабируемой по силе попадания;
компактный блок «Ход боя» с тремя последними действиями без чисел.
Часть крови после попадания остаётся на полу примерно на 20% дольше исходного
эффекта, темнеет и плавно исчезает; сами пятна увеличены на 20%, принадлежат
только визуальному движку и не меняют replay.
Под живым бойцом в раненом состоянии дополнительно постоянно чередуются две
падающие капли и короткий след их контакта с землёй.
Полный технический журнал не дублируется в телефоне и остаётся в диагностике
ниже сцены. Фабрика движка и стартовая форма используют одинаковые значения
здоровья: `200` у Тита-ретария и `240` у Маркуса-мурмиллона.
Допустимый диапазон базового здоровья в форме и движке — `1…500`.
Расширенный режим калькулятора допускает до `2000` шагов, силу и харизму до
`500`, множитель арены до `10`, а критический и классовый шансы — до `100%`.
Значения `75%` и `100%` предназначены для диагностики анимаций и механик;
балансовые значения реального боя должны оставаться существенно ниже.

«Ход боя» использует общий презентационный адаптер `battle-story.js`. В
начальном snapshot он показывает только временные баффы и стартовые травмы
бойцов — постоянные перки исключены. Полученная во время боя травма добавляется
отдельной строкой сразу после соответствующего действия и больше не повторяется.
На левой и правой сторонах пергамента ниже `70%` здоровья начинает проявляться
первое полупрозрачное багровое пятно соответствующего бойца. По мере ухудшения
оно растёт и становится ярче, затем в фиксированных точках появляются ещё три
пятна. Их центры не смещаются. Каждое пятно получает стабильный случайно
выглядящий предел роста, поэтому они не достигают максимума одновременно.
Переход появления длится `0,7 с`, все слои остаются под текстом. Критическая
запись крупнее и выделена тёмным багровым цветом. Левая и правая композиции
намеренно не зеркальны. Ближе к внешним углам по отдельным порогам появляются
три мелкие яркие брызги; после появления их размер и яркость не меняются.

В настройках каждого бойца отдельно выбирается шанс критического удара:
`0%`, `3%`, `5%`, `10%`, `30%`, `50%`, `75%` или `100%`. Дефолт — `3%`. Выбранная доля входит
в нормализованный input, снимки и техническое событие расчёта удара; крит
по-прежнему удваивает итоговый урон.

Рядом расположен такой же компактный выбор шанса классового приёма с дефолтом
`10%`. Проверка выполняется только при подходящем событии класса, записывает
`classTechniqueChance` и `classTechniqueRoll` в технический replay, а значение
`0%` полностью отключает срабатывание техники на текущий бой.
У Мурмиллона успешная проверка сначала превращает попадание противника в блок,
а его следующий удар получает усиление `×1,25` и проигрывает строку `special`.
После ответного удара связка снова готова к следующему подходящему попаданию;
одновременно может быть подготовлена только одна контратака.

### Как анимируются будущие ассеты

Текущий набор мечника `unified v21` — прозрачный атлас
[`unified-swordsman-grid-v21.png`](assets/unified-swordsman-grid-v21.png) размером
2304×5376: 6×14 физических кадров по 384×384. Строка 11 содержит усиленный
классовый удар, строка 12 — пробную оглушённую стойку, строка 13 — удар с
разворота. Ретиарий пока использует
совместимый атлас `unified-retiarius-grid-v12.png` размером 2304×4992 с сеткой
6×13. Строка 11 содержит усиленный удар трезубцем с короткого прыжка, строка 12 — оглушение. У обоих листов физическая
ячейка содержит прозрачный буфер, а логическая ячейка тела остаётся 256×256. В каждом
кадре уже находятся тело, броня и классовая экипировка. Исходное направление —
вправо; второй боец зеркалится рендерером.

Базовые одиннадцать строк генерируются отдельно: `idle.normal`, `idle.tired`,
`idle.injured`, `attack`, `defense.block`, `defense.dodge`,
`reaction/defeat`, `movement`, `greeting`, `victory` и `special`. Для мечника к ним
добавлены `special.enhanced`, `reaction.stunned` и `attack.spinning`; ретиарий
также получает собственный `special.enhanced` в строке 11. Итоговая матрица
мечника пересобирается скриптом
[`tools/build_unified_swordsman_atlas.py`](tools/build_unified_swordsman_atlas.py).
Совместимый лист ретиария собирается
[`tools/build_unified_retiarius_atlas.py`](tools/build_unified_retiarius_atlas.py)
из построчных исходников. Общий сборщик находит шесть основных силуэтов в каждой
строке, удаляет шахматный фон и его светлый matte, восстанавливает тёмный внешний
пиксельный контур и вычисляет эталонную визуальную массу по `idle.normal`.
Каждый исходный кадр нормализуется к этому эталону по плотной части тела без
тонких штрихов оружия. Кадры падения наследуют калибровку стоячей реакции на
удар: уменьшение видимой площади лежащего тела не увеличивает труп. Сборщик
проверяет отношение ширины финальной лежащей позы к эталонному росту.
Нормализация запекается в PNG, поэтому runtime всегда
рисует кадры с одним неизменным масштабом. Если любой силуэт не помещается, сборка
завершается ошибкой вместо его скрытого уменьшения. После этого каждый кадр
упаковывается в ячейку 384×384 с буфером 128 px сверху и 64 px по бокам. Рендерер считает
нормальным размером только логические 256×256; трезубец может занимать буфер и
не уменьшает тело. Затем сборщик проверяет размеры, безопасные поля, число кадров и крупные
оторванные компоненты. Поэтому ошибка одного кадра не сдвигает соседние кадры.
Для первых трёх кадров `reaction.hit` корень вычисляется по геометрическому
центру обеих стоп, а не по медиане пикселей одной более заметной ноги. Кадры
падения 3–5 центрируются отдельно; проверка сборки запрещает дрейф стоячего
корня больше одного пикселя. Масштаб тела стоячей реакции также нормализуется
для каждого кадра отдельно. В победной строке рост измеряется по плотному телу
без вертикально поднятого оружия, поэтому салют не уменьшает бойца.

`ANIMATION_SPRITE_ROWS` задаёт строки, а не просто номера кадров:

| Строка | Состояние | Кадры | Тип |
|---:|---|---:|---|
| 0 | `idle.normal` | 6 | loop — лёгкое дыхание |
| 1 | `idle.tired` | 6 | loop — тяжёлое дыхание |
| 2 | `idle.injured` | 6 | loop — бережёт рану |
| 3 | `attack` | 6 | полный удар: замах → контакт → возврат |
| 4 | `defense.block` | 6 | быстрый блок · 20 FPS / 300 мс |
| 5 | `defense.dodge` | 6 | уход / backstep |
| 6 | `reaction.hit`, `defeated` | 6 / 3 | попадание и падение |
| 7 | `advance`, `retreat` | 6 | движение; назад проигрывается в обратном порядке |
| 8 | `greeting` | 6 | приветствие соперника перед сближением |
| 9 | `victory` | 6 | циклический победный салют поднятым оружием |
| 10 | `special` | 6 | особый приём: силовой прыжок мечника или бросок сети ретиария |
| 11 | `special.enhanced` | 6 | усиленный классовый удар: мечник бьёт мечом, ретиарий атакует трезубцем с короткого прыжка |
| 12 | `reaction.stunned` | 1→2→3→5→3→5→3→6 | мечник и ретиарий: зацикленно приседают и прикрывают голову; кадр 4 пропускается |
| 13 | `attack.spinning` | 6 | только мечник: полный разворот корпуса и широкий рубящий удар |

Полный набор состояний также включает `advance`, `greeting`, `victory` и `special` для
сближения, результата боя и классовых приёмов. Визуальный адаптер выбирает
`special` для Прыжка Ахилла, активного классового приёма или атакующего
оружейного перка. Защитная техника Мурмиллона остаётся быстрым
`defense.block`. Сгенерированные листы пока помечены `experimental`: сборщик
проверяет размеры, безопасные поля и baseline, но перед production-релизом
генеративные placeholder-кадры всё равно нужно заменить финальным артом.

Отдельная страница [`reference.html`](reference.html) содержит справочник классов,
оружия, доспехов, классовых приёмов, постоянных и временных перков, травм и эффектов
редкого снаряжения. Для каждого класса показаны конкретные предметы четырёх качеств
с характеристиками и описанием при наведении. Страница не запускает симуляцию и
описывает целевую модель `0.6`.

### PixiJS: скелетный эксперимент

Под мобильным предпросмотром есть отдельный блок **«Скелетная анимация»**. В нём
`weapon` является дочерним элементом предплечья, поэтому меняется независимо от
тела. Это намеренно простая кодовая кость, а не интеграция Spine: она сравнивает
гибкость рига с качеством покадрового pixel art до выбора финального пайплайна.

Автономный отладочный интерфейс для текущей [боевой спецификации](../../combat-spec/README.md)
и [архитектуры движка](../../architecture/battle-engine.md). Полный контракт
мобильного экрана, слоёв, фонов и визуального давления инициативы/здоровья вынесен в
[визуальную спецификацию](../../architecture/battle-visual.md). Внешние зависимости и
сборка не требуются. Прототип работает как через локальный сервер, так и при открытии
`index.html` напрямую через `file://`.

## Запуск

```bash
cd prototypes/battle-arena
npm start
```

Открыть [http://localhost:8765](http://localhost:8765).

Либо открыть `index.html` двойным кликом. Обычные скрипты используются намеренно:
локальные ES-модули блокируются браузерами при запуске через `file://`.

Проверка чистого движка:

```bash
npm test
```

## Возможности

- настройка базовой силы, здоровья и харизмы двух бойцов;
- выбор одного из пяти классов и отдельных комплектов оружия и доспехов;
- четыре качества предметов с фиксированными характеристиками, иконками, классовым
  приёмом и дополнительными эффектами редких и именных предметов;
- два типа арены: обычная и песчаная, а также два множителя поддержки;
- воспроизводимый `seed` и максимальное количество шагов;
- три слота перков для каждого бойца с возможностью оставить слот пустым;
- неограниченные динамические списки временных эффектов и стартовых травм;
- минимальная Canvas-арена: чёрный фон, цветные полоски бойца и оружия, HUD и автоматический показ каждого хода раз в 1,5 секунды;
- независимые движения бойца и оружия при атаке, прыжке, промахе, увороте, блоке и получении удара;
- отдельная библиотека скинов-заглушек, готовая к подключению спрайт-листов;
- пауза, продолжение, повтор боя, ручные стрелки и переход к любому снимку ползунком;
- случайный визуальный интервал между действиями от 1× до 3× базовой паузы;
- точные динамические характеристики бойцов;
- итог боя, выживание и травмы;
- агрегированная статистика;
- полный технический лог фаз, hook-методов, бросков и эффектов;
- полное состояние системы после каждого события: бойцы, арена, очередь эффектов и runtime модификаторов арены;
- переход к точному состоянию события при раскрытии записи журнала;
- скачивание самодостаточного журнала боя в JSON для будущего replay и «машины времени».

## Классы и экипировка

Прототип `0.6` реализует пять классов: **Мурмиллон**, **Фракиец**, **Ретиарий**,
**Секутор** и **Гопломах**. Вход движка хранит класс в `fighterClass`, а предметы —
в отдельных слотах `weaponSet` и `armorSet`. Чужой классу предмет не применяется:
нормализация выбирает допустимый обычный комплект этого класса.

Каждый оружейный комплект создаёт модификатор арены с
`kind = "class-technique"`. Редкое и именное оружие добавляет 1–2 модификатора
`equipment`, редкие и именные доспехи — один защитный модификатор. Числовые
показатели предметов участвуют в уроне, блоке, точности, усталости, увороте и
инициативе. Матрица преимуществ пяти классов даёт `×1.15` к силе и `+10`
инициативы выгодной стороне.

Все перки, бафы, травмы, классовые приёмы и эффекты предметов выполняются через
единый интерфейс `BattleModifier`; события журнала называются `modifier.hook`,
`modifier.activated` и `modifier.runtime.changed`.

## Реализованные перки

| Перк | Hook | Эффект |
|---|---|---|
| Загнанный зверь | `afterRecalculateStrength` | При здоровье не выше 35% даёт +50% силы |
| Крепкие кости | `afterAction`, `beforeOutcome` | Уменьшает вероятность травмы на 65% |
| Любимец толпы | `afterRecalculateSupport` | После пересчёта добавляет 6 поддержки |
| Перехват хода | `afterSelectActor` | Один раз заменяет уже выбранного бойца владельцем перка |
| Лёгкая поступь | `beforeApplyEffects` | Уменьшает получаемую усталость на 25% |
| Рыжий | `beforeInitialize` | Понижает харизму на 10 |
| Умелый воин | `afterAction`, `afterSelectActor` | Запоминает успешную защиту или промах врага и гарантирует следующий ход |
| Позёр | `afterAction` | Получает поддержку за блок или уворот ценой усталости |
| Любимец неудач | `afterAction` | Получает поддержку после собственного промаха |
| Прыжок Ахилла | `beforeAction` | С шансом 10% выполняет гарантированный удар сверху с +50% силы; его нельзя заблокировать или избежать уворотом |

## Временные эффекты и стартовые травмы

Все элементы перехватывают `beforeInitialize`, поэтому их применение и порядок
видны в техническом логе. Повторяющиеся временные эффекты разрешены и складываются.
Во входе и результате стартовые травмы хранятся отдельно от новых травм, полученных
во время или после боя.

Перед боем в симуляторе для первого бойца выбирается до трёх разных ручных
боевых бафов; пустой набор тоже допустим. Мобильная кнопка над журналом показывает
только выбранные усиления.
Команда сохраняется в `playerBuffCommands` вместе с границей итерации и при каждом
изменении детерминированно пересчитывает оставшуюся часть реплея. `Соберись!`
снимает 35% усталости, `Вперёд!` даёт временную инициативу с последующим штрафом,
а `Сейчас!` создаёт отдельную итерацию гарантированного классового удара с
множителем силы `×1.25` и оглушает цель
на два следующих выбора действия. `Держись!` выдаёт отдельный слой временного
здоровья в размере 20% максимального на 5 итераций. Урон сначала расходует этот
слой; после его истечения остаток исчезает, а входящий урон до конца боя
увеличивается на 25%.

Временные эффекты: **Эффект бани**, **Вино**, **Сытный обед**,
**Разминка с тренером**, **Благословение жреца**, **Боевой настой** и
**Хороший сон**.

Стартовые травмы: **Повреждение ноги**, **Повреждение руки**,
**Повреждение головы**, **Ушиб рёбер** и **Истощение**.

Результат одного hook-метода передаётся следующему перку. Изменения состояния
фиксирует движок после завершения цепочки.

## Временные правила прототипа

Это рабочие решения для открытых вопросов, а не финальный баланс:

- инициатива зависит от поддержки, силы, усталости и травм ноги;
- следующий боец выбирается случайно с весом, равным инициативе;
- исход атаки выбирается между промахом, уворотом, блоком и попаданием;
- удар, уворот и блок увеличивают усталость;
- усталость снижает эффективную силу конкретного удара;
- броня уменьшает урон и повышает вес блока;
- тяжёлое попадание может дать травму руки или ноги;
- множитель арены применяется к начальной поддержке и изменениям поддержки;
- тип арены пока не участвует в расчётах, но доступен перкам и влияет на визуал;
- при достижении лимита шагов без победителя фиксируется ничья;
- гибель и итоговая травма рассчитываются отдельно после результата боя.

Все вероятности, веса, случайные броски, входы и результаты фаз сохраняются в
техническом логе, поэтому формулы можно менять и сравнивать на одинаковом `seed`.
